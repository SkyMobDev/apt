# The publish workflow parses stable.list line by line on Linux; a CR riding
# along inside a version would end up in the release tag it builds. Normalize
# everything in this repository to LF so a Windows checkout cannot introduce one.
* text=auto eol=lf
