# Build output of .github/workflows/publish.yml. The pool is re-downloaded
# from the upstream releases on every run, so no .deb belongs in git history.
/_site/
/dists/
/pool/
/Release.tmp

# Save-AptCandidate drops release builds here for testing before promotion.
/candidates/
