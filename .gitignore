# Build output of .github/workflows/publish.yml. It downloads the .deb files
# from the `pool` release into downloads/ and lays them out under pool/.
/_site/
/dists/
/pool/
/downloads/
/Release.tmp
/filelists/
/ftparchive.conf

# Save-AptCandidate drops release builds here for testing before promotion.
/candidates/
