# Build output of .github/workflows/publish.yml. pool/ is NOT here: the .deb
# files are committed, which is what lets CI publish without any credential
# for the private repository.
/_site/
/dists/
/Release.tmp

# Save-AptCandidate drops release builds here for testing before promotion.
/candidates/
